name: starter
on:
  push: [$default-branch]
  workflow_dispatch:

job:
  az-cli:
    name: "azure setup"
    runs-on: ubuntu-latest
  steps:
    - name: setup
      run: az cli

    - name: print vars
      run: echo vars
    
  
